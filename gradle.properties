# Completely suppress the deprecation message text blocks from the console
org.gradle.warning.mode=none

# Enable the configuration cache to silence the hint and dramatically accelerate subsequent builds
org.gradle.configuration-cache=true
